# Environment secrets
.env
.env.local

# Runtime data (generated on container start)
config/
files-storage/

# Wallpapers subidos por usuarios (no deben entrar en la imagen)
panel/public/wallpapers/*/

# Docker override (local dev only)
docker-compose.override.yml

# OS
.DS_Store

# Local project docs
PROJECT_MAP.md
IDEAS.md
