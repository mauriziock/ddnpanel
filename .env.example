# Environment Variables for DDN Control Panel

# Authentication Secret (Generate a strong random string)
# You can generate one with: openssl rand -base64 32
AUTH_SECRET=

# Public URL (Optional, but recommended for production behind proxy)
# NEXTAUTH_URL=https://panel.example.com

# Trust Host (Required if behind a proxy like Nginx/Traefik where Host header is forwarded)
AUTH_TRUST_HOST=true

# Node Environment
NODE_ENV=production
